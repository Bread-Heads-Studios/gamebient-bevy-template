# Fleet catalog — 2026-09-16 survey

Source: `.superpowers/sdd/2026-09-16-replay-rollout/survey-2026-09-16.md`
(18 Bevy 0.18 games; `delivery-dash`, `pizza-rush` are Bevy 0.15 and out of
scope, as are `voidrunner-authoritative`/`voidrunner-streamed`). Scope of
the survey: `src/` excluding `src/ui/`, `record/`, `autopilot.rs`, tests,
dev harnesses. No game uses `Instant::now`/`SystemTime`/frame count; no
external physics crate anywhere; no game iterates a `HashMap`/`HashSet` in
gameplay (lookup-only uses are noted below and are fine as-is).

Wave order is value-first (published S → M → L, then unpublished) per the
design spec's Decisions table. Pick the next `[ ]` row in this order unless
told otherwise (skill step 1). Tick a row (date, PR, build id, run id) in
skill step 9, after production proof.

## Status

| Wave | Game | Status |
|---|---|---|
| 0 | cannonball-putt | [x] 2026-09-17 — PR #3 (build 0.1.0+8fd4e63); first verified production run rank #1, 2300 pts, 2026-09-17T15:15Z. Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (cannonball-putt#7) |
| 1 | dough-io | [x] merged + owner production run verified 2026-09-18 — https://github.com/Bread-Heads-Studios/dough-io/pull/3. Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (dough-io#5) |
| 1 | grand-theft-auto-reply | [x] merged + owner production run verified 2026-09-18 — https://github.com/Bread-Heads-Studios/grand-theft-auto-reply/pull/3. Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (grand-theft-auto-reply#6) |
| 2 | pack-the-ripper | [x] merged 2026-09-18 (pack-the-ripper#3, `86e9b06`). Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (pack-the-ripper#4); **production proof done 2026-09-22** — owner-played run on colecovisiongx.com showed the Verified banner |
| 2 | sundae-shooter | [x] merged 2026-09-18 (sundae-shooter#3). Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (sundae-shooter#4); **production proof done 2026-09-22** — owner-played run on colecovisiongx.com showed the Verified banner |
| 2 | attic-excavator | [x] merged 2026-09-18 (attic-excavator#3, `40633d9`). Upgraded to template d806aa0 with the windowed_shape probe 2026-09-18 (attic-excavator#4); **production proof done 2026-09-22** — owner-played run on colecovisiongx.com showed the Verified banner |
| 3 | gulper | [x] merged 2026-09-22 (gulper#5, `caa5940`); upgraded to 17ea13b; **production proof done 2026-09-24** — owner-played run on colecovisiongx.com showed the Verified banner |
| 3 | ladder-legend | [x] merged 2026-09-22 (ladder-legend#5, `3af6306`); upgraded to 17ea13b; **production proof done 2026-09-24** — owner-played run on colecovisiongx.com showed the Verified banner |
| 3 | grand-theft-otto | [x] merged 2026-09-22 (grand-theft-otto#3, `330802d`); upgraded to 17ea13b; **production proof done 2026-09-24** — owner-played run on colecovisiongx.com showed the Verified banner |
| 3 | dive-rise | [x] merged; production mismatch 2026-09-23 root-caused (app-lifetime `Time<Fixed>::elapsed` in nine sim systems) and fixed in dive-rise#5; upgraded to 17ea13b (dive-rise#6, `61b10b7`); **production proof done 2026-09-24** — owner-played run on colecovisiongx.com showed the Verified banner |
| 4 | tire-stack | [x] merged 2026-09-24 (tire-stack#6, `2b1c9d7`) — unpublished. Owner-visible: **the autopilot loses through input** — from t > 54 s it wrecks the load and the real game-over path ends the run, instead of writing `data.lives = 0`; the stack lean left the sim and is painted windowed-only |
| 4 | BeerPong | [x] merged 2026-09-24 (beer-pong#8, `e539fa0`) — unpublished. Owner-visible: **a pause no longer cancels an in-progress charge**, and **each turn is ~2 ticks shorter** (`ThrowPhase` became a resource, so a phase change lands inside the tick that decides it rather than on the next frame) |
| 4 | Gravestone_Gauntlet | [x] merged 2026-09-24 (graveyard-gauntlet#9, `7fe6d43`) — unpublished. Owner-visible: **`PlayState` is gone, so the boss card and wave transitions are hard cuts** (no fade sub-state); collision responses land on the tick they were detected; the autopilot ends through input and its tour can now end early |
| 4 | voidrunner | [x] merged 2026-09-24 (voidrunner#16, `283c173`) — unpublished. Owner-visible: **`PlayPhase` is gone, so the leg transitions (corridor → arena → boss) are hard cuts** where the sub-state fade used to wipe; the phase card, music, boss health bar and starfield now follow a resource from `Update` |
| 4 | beat-bender | [x] merged 2026-09-24 (beat-bender#4, `d3935e5`; the record harness was rolled out in the same PR) — unpublished. Owner-visible: **the song/difficulty picker moved off the title screen into the run** as a "CHOOSE YOUR FIGHT" card, and **charts now vary per `RunSeed`** (`gen_chart`'s seed is the song constant XOR a `GameRng` draw) — re-shoot the cover/video notes if either shows the old title screen |
| 5 | pizza-pinball | [ ] |
| 5 | Hunted | [ ] |
| 5 | moleman-racing | [ ] |

**Production proof (skill step 8) is done for waves 0-3** — an owner-played
run on `colecovisiongx.com/play/<pda>` showing `Verified · <score> · #<rank>`
and a `POST /api/runs` run id, for all ten published games (waves 0-2 on
2026-09-17/18/22, wave 3 on 2026-09-24).

**For wave 4 it is not possible yet, and that is not a gap in the ports.**
All five games are unpublished: no PDA, nothing in the current publish list,
so there is no `colecovisiongx.com/play/<pda>` to play. Each one merged with
green gates, a real rendered run verified natively **and** under Node with
identical checksums, and `properties.verify_url` sitting in
`assets/info.json` ready for the day it is minted. Step 8 is outstanding for
all five and belongs with publishing, not with the port — batch it then, and
check the Vercel production deploy at the same time (`build_web.sh` now
builds `--locked`, so a stale lock fails the deploy rather than re-resolving
it).

**All ten wave-0-3 games are on template `17ea13b` as of 2026-09-23** — see
"Done: fleet upgrade to template 17ea13b" below for the per-game table, what
each one needed, and the three follow-ups it left. The five wave-4 ports were
made against template `e789fd6` and left the template untouched; this
document is the first thing that folds them back in.

## Done: fleet audit 2026-09-23 — app-lifetime clock reads

Dive Rise's production `UNVERIFIED` was `Time<Fixed>::elapsed()` read from
nine sim systems: it counts from **app** start, so a run forked on how long
the player sat in the menu, while the verifier always re-simulates at offset
zero. Fixed in dive-rise#5 and folded into the template (rule 7,
`sim::RunClock`, the `record_windowed_after_menu` row and a
`tools/rollout-replay.sh` advisory).

**All nine verified games were audited on 2026-09-23.** One PR per game, all
open and awaiting review at the time of writing; each adds the after-menu
row and a `no_app_lifetime_clock_in_sim_code` source scan whatever it found,
so a clean game still gained the two guards.

| game | PR | verdict | what it changed |
|---|---|---|---|
| grand-theft-auto-reply | [#8](https://github.com/Bread-Heads-Studios/grand-theft-auto-reply/pull/8) | no sim clock read — **but found the second bug class** | `selection::handle_input`'s two `Local<Repeat>`s → `selection::RepeatState`, cleared by `reset_run` |
| pack-the-ripper | [#6](https://github.com/Bread-Heads-Studios/pack-the-ripper/pull/6) | **one real sim read** | `conveyor::move_belt` → game-local `run_clock::RunClock`; checksum-invisible today, latent anyway (see below) |
| ladder-legend | [#7](https://github.com/Bread-Heads-Studios/ladder-legend/pull/7) | clean | guards + a marker on `pulse::camera_pulse` (presentation) |
| cannonball-putt | [#9](https://github.com/Bread-Heads-Studios/cannonball-putt/pull/9) | clean | guards; `Local`s only in `ball::spawn_trail` (`Update`) and `host::report_score` |
| sundae-shooter | [#6](https://github.com/Bread-Heads-Studios/sundae-shooter/pull/6) | clean | guards + markers |
| attic-excavator | [#6](https://github.com/Bread-Heads-Studios/attic-excavator/pull/6) | clean | guards + markers |
| dough-io | [#7](https://github.com/Bread-Heads-Studios/dough-io/pull/7) | clean | guards + markers; the sanctioned clock is `pace::RunClock` |
| gulper | [#6](https://github.com/Bread-Heads-Studios/gulper/pull/6) | clean | guards; the replay port had already moved these reads |
| grand-theft-otto | [#5](https://github.com/Bread-Heads-Studios/grand-theft-otto/pull/5) | clean | guards; `Local`s only in `host.rs`, `record/` and the selftest bot |

So the original grep counts (gulper 9, attic-excavator 7, pack-the-ripper 6,
dough-io 6, sundae-shooter 4, grand-theft-auto-reply 2, ladder-legend 1) were
presentation and dev-harness hits in all but **one** case: pack-the-ripper's
`conveyor::move_belt`. The suspects called out beforehand — `gulper/band.rs`,
`sundae-shooter/launcher.rs`, `pack-the-ripper/sorting.rs` — were all
`Update` systems. Classification is by **where the system is registered**,
never by what the name suggests.

**The audit's real yield was the second bug class**, which no one was looking
for: run state that outlives the run. Grand Theft Auto-Reply kept a cursor
auto-repeat in a `Local<_>` inside a `SimSet` system, so a second run in one
browser session began with run 1's last held direction and its repeat timer,
and diverged from the verifier's fresh app on tick one. Folded into the
template as `sim::tests::no_local_state_in_sim_systems`, a
`tools/rollout-replay.sh` advisory, and
`tests/windowed_shape.rs::a_second_run_in_the_same_app_reproduces_the_first`
— the first probe anywhere in the fleet that records **two consecutive runs
in one `App`**. Port checklist rule 7, "Run state may not outlive the run",
has the worked example and the per-game table.

**And pack-the-ripper's site is the lesson about latency.**
`conveyor::move_belt`'s clock reached only a pack's `Transform.rotation`,
which nothing reads back and the checksum does not fold — measured, by
reverting the fix and watching the after-menu row pass. A real divergence
the checksum cannot see today is still a latent `UNVERIFIED`: the day
anything folds that rotation or derives a position from it, every run
recorded after a menu dwell is unreproducible locally. Fix by registration,
not by whether today's fold happens to notice.

**Follow-up, not yet done: converge the game-local `run_clock.rs` modules.**
Eight of the nine PRs added a game-local `src/game/run_clock.rs` holding the
source scan (and, in pack-the-ripper, a game-local `RunClock`), because they
were written against game copies that predate template `7fd5285`. A fleet
`--upgrade` to template ≥ `7fd5285` refreshes the verbatim `sim.rs` and
brings `sim::RunClock` + `sim::sync_run_clock` + `sim::tests::
no_app_lifetime_clock_in_sim_code` with it, at which point those modules are
**redundant duplicates**. Keep or delete per game — a duplicate scan is
harmless, two `RunClock`s are confusing — but do it deliberately and in one
pass, and in pack-the-ripper's case point `move_belt` at `sim::RunClock.secs`
and drop the local resource so nothing wires a second clock into the chain.
The same upgrade is what carries the second-run row out to the fleet:
`--upgrade` will not rewrite an adapted `windowed_shape.rs`, so each game
gets a `HAND EDIT` naming the row until it is ported across by hand.

**Dive Rise is the one game that diverges.** It shipped the fix first,
as `run::RunClock(pub f32)` in its own `src/game/run.rs` with
`run::sync_run_clock`; the template's is `sim::RunClock { secs, ticks }` in
the verbatim-copied `sim.rs`. Its next `--upgrade` will therefore hand it a
*second*, unused clock and a duplicate source-scan test. Converge it then:
point the nine systems at `sim::RunClock.secs`, delete `run::RunClock`,
`run::sync_run_clock` and `run::tests::no_app_lifetime_clock_in_sim_code`,
and keep `sim::sync_run_clock` second in the chain. Bit-neutral either way —
both restate the same number from the same tick index.

The recipe the nine audits followed, for a game not on the list (wave 4 and
5 games get it as part of the port): run `tools/rollout-replay.sh --upgrade`
to pick up `sim::RunClock` and both probes, read every hit the two advisories
name and classify each by **where its system is registered**, replace the sim
ones with `RunClock` and the sim `Local`s with run-scoped resources, then
re-run the after-menu and second-run rows. **They only prove something on a
long, busy run** — a 1 720-tick Dive Rise run that ate nothing verified green
on the broken build at eight different clock offsets. The wave-0/1/2
production proofs stand: pack-the-ripper's was the only sim-registered hit
found, and its divergence does not reach that game's checksum.

**Next:** wave 5 — `pizza-pinball`, `Hunted`, `moleman-racing`. All three
are L, all three unpublished, and each needs something *before* the port
proper rather than during it:

* **`pizza-pinball`** — tick-sensitive physics and **33 transcendental call
  sites**, the last of the two high-libm games now that Voidrunner is done.
  Read rule 5's transcendental rule first: fold nothing `libm` produced, and
  turn every constant-argument decay into a `*_PER_TICK` literal unit-tested
  against the tick length at `1e-6`. Voidrunner's 35 sites cost no drift at
  all because nothing libm-derived reaches its checksum — that is the bar.
* **`Hunted`** — flat layout and **no `assets::AssetsPlugin`**, so
  `tools/rollout-replay.sh` refuses to write `tests/windowed_shape.rs`
  rather than hand over a file that cannot compile (test part (p) pins that
  refusal). Its probe has to be **hand-written** against whatever this
  game's real `!headless` shape is. Budget for it; it is the test that
  catches the fade and app-clock classes, so "skip it" is not an option.
* **`moleman-racing`** — **0 `GameInput` uses.** Gameplay reads raw
  `ButtonInput<KeyCode>` through a custom `Controls` resource
  (`game/controls.rs:33-44`, populated in `PreUpdate`). A sim that reads raw
  keys records nothing a replay can carry, so **bridge `Controls` onto the
  canon first**, as its own change: Ladder Legend's `input::mat_panel_bridge`
  is the pattern (latch onto `VirtualInput` in `PreUpdate`
  `.before(accumulate_input)`), after which the sim reads `TickInput` like
  every other game. Then port.

`Hunted` and `moleman-racing` have sections in `references/irregular-games.md`;
read them with the row.

**After wave 5 the rollout is complete.** `delivery-dash` and `pizza-rush`
are Bevy 0.15 and out of scope, as are `voidrunner-authoritative` and
`voidrunner-streamed`.

## Done: fleet upgrade to template 17ea13b — 2026-09-23

The follow-up the audit section above asked for, done in one pass. All ten
verified games are on template `17ea13b`: `sim::RunClock` +
`sim::sync_run_clock` (template#23), the second-run probe row and
`sim::tests::no_local_state_in_sim_systems` (template#24), and the wave-3
rules (template#22). Every one of them deleted its game-local
`src/game/run_clock.rs` — the template's `sim.rs` now carries the same scan
over the same tree with the same `allow-app-clock` marker — and **not one
regenerated a fixture**: `RunClock` was bit-neutral in every game, either
because nothing read it or (Dive Rise) because the template's expression was
a copy of the game's own.

| game | PR | merge | what it needed beyond the mechanical upgrade |
|---|---|---|---|
| cannonball-putt | [#10](https://github.com/Bread-Heads-Studios/cannonball-putt/pull/10) | merged `1c9f659` | bot rewind (its script reads `Shot`/`GameData`, not the tick); `runs` guard moved after the verdict; keeps `hazards::TiltClock` |
| pack-the-ripper | [#7](https://github.com/Bread-Heads-Studios/pack-the-ripper/pull/7) | merged `7a9b581` | `selftest::script`'s grade counter `Local<u32>` → `selftest::ScriptGrades` (it picks the two deliberate misgrades out of a run, so run 2 played clean); `conveyor::move_belt` onto `sim::RunClock` |
| grand-theft-auto-reply | [#9](https://github.com/Bread-Heads-Studios/grand-theft-auto-reply/pull/9) | merged `10c8f60` | **the holder-bot finding**: its own `Local<Repeat>` passes the row under a tapping bot with the fix reverted. `holder_script` holds DOWN every tick and keeps the fixture's taps |
| attic-excavator | [#7](https://github.com/Bread-Heads-Studios/attic-excavator/pull/7) | merged `bf1af60` | board-regeneration assertions (`AtticGrid::cells_hash()` folded with `CurrentLevel`, checked *before* ticks/score/checksum so a mismatch names its cause); row runs under both bots |
| dough-io | [#8](https://github.com/Bread-Heads-Studios/dough-io/pull/8) | merged `1e86496` | wiring by hand (`GamePlugin` lives in `sim_core.rs`, so the script's insertions were false positives); keeps `pace::RunClock`; no holder needed — its script writes a normalised axis every tick and is already one |
| sundae-shooter | [#7](https://github.com/Bread-Heads-Studios/sundae-shooter/pull/7) | open, merging | `replay::selftest::BotState` `Local` → resource; a second `…_with_the_stick_held` row for `launcher::aim_launcher`'s angle integrator; **measured the formula difference** that became template#25 |
| ladder-legend | [#8](https://github.com/Bread-Heads-Studios/ladder-legend/pull/8) | open, merging | `play_one_run` applies `StateTransition` by hand (both bots read the live note field); the dwell moved into the real `Menu` diorama; `Bot::Holder` written with `latched` first and caught by its own vacuity guard |
| gulper | [#7](https://github.com/Bread-Heads-Studios/gulper/pull/7) | open, merging | `ScreenFade::boot()` re-staged per run; `death::restore_time` registered in the probe app; holder = fixture + held A (3 818 tune frames against 0 for two other shapes); **the flaky-counter finding** |
| grand-theft-otto | [#6](https://github.com/Bread-Heads-Studios/grand-theft-otto/pull/6) | open, merging | `OnExit(Menu)` `GameEntity` sweep moved into the common half of `GamePlugin::build`; `VirtualInput` cleared between runs; reckless bot's `Local<u32>` → `RecklessFrame`; **found the rollout-script `Mute`-arm bug** |
| dive-rise | [#6](https://github.com/Bread-Heads-Studios/dive-rise/pull/6) | open, merging | the clock comes home: nine phase sites off `run::RunClock` onto `sim::RunClock.secs`; `Bot::Coaster` row over `player::Facing` |

**Everything above is folded into the template** by the PR this section
ships with: the `Bot::Holder` row and the holder mutation recipe, the
`play_one_run` rewinds (bot state, `VirtualInput`, the per-run fade, the
hand-run `StateTransition`), the `runs`-guard ordering, the
presentation-counter rule, the `OnExit(Menu)` rule, and a fix for the
`rollout-replay.sh --upgrade` bug Grand Theft Otto found — its idempotency
guard on `src/game/host.rs`'s `Mute` arm keyed on the script's own
`as_deref_mut()` handwriting rather than on "is this assignment guarded at
all", so a game that had hand-written `global_volume.as_mut()` got its arm
wrapped a second time into something that does not compile.

### Follow-ups

* **Dive Rise: regenerate both fixtures under template#25's formula.** #25
  landed `secs = ticks as f32 / TICK_HZ as f32` *after* dive-rise#6 was
  written against the `Duration` expression. Dive Rise is the one game in
  the fleet whose sim systems actually read `RunClock.secs` (nine of them),
  so for it alone the change is not bit-neutral and both `.gxr` files have
  to be re-recorded. In flight at the time of writing.
* **Gulper: migrate `game::sim_secs` onto `sim::RunClock.secs`.** Gulper
  kept its own `ticks as f32 / 60.0` clock because the template's
  `Duration` expression differed from it in the last bit on 24.5% of tick
  indices, and left a test —
  `game::tests::sim_secs_is_close_to_but_not_bit_equal_to_the_template_run_clock`
  — that goes red the day the template computes `ticks / TICK_HZ`. #25 is
  that day, so that test is now the to-do: the migration is bit-neutral,
  delete `sim_secs` and the test with it. Same for Sundae Shooter's
  `juice::update_bobs`.
* **Production proof still outstanding for every wave-3 game** (gulper,
  ladder-legend, grand-theft-otto, dive-rise), plus **dive-rise's re-proof**
  after the clock fix. Unchanged by this wave: none of these PRs moved a
  checksum, so nothing already proved was invalidated.

## Fleet order, size and metadata

| Wave | Game | Path | Published | Size | Collection PDA |
|---|---|---|---|---|---|
| 0 | Cannonball Putt | `games/cannonball-putt` | Y | S | `BrgzzBTfV2soCeZxWRKz2LBKNfCPdjwoCUqk6PwfCZcL` |
| 1 | Dough.io | `games/dough-io` | Y | S | `2L7MrLva2rRvcj9x26kC53zaA3YEscjzBKXTiAU9kdTu` |
| 1 | Grand Theft Auto-Reply | `games/grand-theft-auto-reply` | Y | S | `Acqc4a5NPsvkTrsYPNgPy1J1KHKeRRAYTVhZ3iJgeJ6H` |
| 2 | Pack The Ripper | `games/pack-the-ripper` | Y | M | `2roYcKk5Pszm1JLC6ym1xhedFft79hqGpcvtc3ZEPGo4` |
| 2 | Sundae Shooter | `games/sundae-shooter` | Y | M | `7TNANAHyZhnPpSPdxxoH9MrzCnFKEAV6NWUh42xnTHsD` |
| 2 | Attic Excavator | `games/attic-excavator` | Y | M | `AvkpSdEN7JSV44CJHJvRh127Hg5NzNYf4bLHpAHSJgnQ` |
| 3 | Gulper | `games/gulper` | Y | L | `Dg2Zmx94vZ69bNFZUm3Bc8X5yUMCFaPzKKBf4YKEiUc` |
| 3 | Ladder Legend | `games/ladder-legend` | Y | L | `H7pcpyQaREfS21YoCeh7ayp8jUgxMQf9jQwUqdAJY4YR` |
| 3 | Grand Theft Otto | `games/grand-theft-otto` | Y | L | `ESZ3snbMVhXQH27RUW1RhCw6nkCPXFFym8gphD4gkAMV` |
| 3 | Dive Rise | `games/dive-rise` | Y | L | `AWp5WuN4kNAEBBu5gZHpHnKiY6McJ2RWC9z8HxPe2VJ9` |
| 4 | Tire Stack | `games/tire-stack` | N | S | — |
| 4 | BeerPong (Table Titans) | `games/BeerPong` | N | S | — |
| 4 | Gravestone Gauntlet | `games/Gravestone_Gauntlet` | N* | M | — |
| 4 | Voidrunner | `games/voidrunner` | N | M | — |
| 4 | Beat Bender | `games/beat-bender` | N | M | — |
| 5 | Pizza Pinball | `games/pizza-pinball` | N* | L | — |
| 5 | Hunted | `games/Hunted` | N | L | — |
| 5 | Moleman Racing | `games/moleman-racing` | N | L | — |

\* Gravestone Gauntlet and Pizza Pinball serve pre-rollout metadata on the
site from an earlier state and are not in the current 10-PDA publish list;
treat them as unpublished for this rollout (per the spec's wave table) and
double check `/library` before assuming otherwise.

## Layout, record/, Update systems

`record/` = has the offline footage recorder rolled out already (see the
`recording-game-footage` skill); missing means run `rollout-record.sh`
first if the port needs it (it doesn't, `rollout-replay.sh` is independent,
but a HAND EDIT will flag it if the recorder's `pub mod record;` line isn't
where the script expects). `Update systems` is `total (Playing-gated)` per
the survey's system count.

| Game | Layout | record/ | lib.rs already split | Update systems | Files with registrations |
|---|---|---|---|---|---|
| cannonball-putt | template | Y | N | 14 (2) | `game/mod.rs` |
| dough-io | template | Y | Y | 6 (3) | `game/mod.rs`, `presentation.rs`, `sim.rs` |
| grand-theft-auto-reply | template | Y | N | 11 (3) | `assets/mod.rs`, `game/mod.rs` |
| pack-the-ripper | template | Y | N | 8 (2) | `game/mod.rs` |
| sundae-shooter | template | Y | N | 20 (3) | `game/mod.rs` |
| attic-excavator | template | Y | N | 15 (3) | `game/mod.rs` |
| gulper | template | Y | N | 46 (5) | `game/mod.rs` |
| ladder-legend | template | Y | N | 28 (4) | `game/mod.rs` |
| grand-theft-otto | template | Y | Y | ~42 behind custom run-conditions `gameplay_live`/`scene_live` (`game/mod.rs:43,49`; `scene_live` also covers Menu attract mode) | `game/mod.rs`, `assets/mod.rs` |
| dive-rise | template | Y | Y | 18 (6) + large satellite modules | `game/mod.rs`, `game/host.rs` |
| tire-stack | template | Y | N | 4 (2) | `game/mod.rs` |
| BeerPong | `game/mod.rs` + GamePlugin | Y | N | 12 (6) | `game/mod.rs` |
| Gravestone_Gauntlet | template | Y | N | 17 (6) | `boss.rs`, `effects.rs`, `enemies.rs`, `host.rs`, `player.rs`, `main.rs` |
| voidrunner | template (`PlayPhase` substate) | Y | N | 43 (6) | `game/mod.rs`, `effects/mod.rs` |
| beat-bender | template | N (missing) | N | 11 (2) | `game/mod.rs` |
| pizza-pinball | template | Y | N | 22 (3) | `game/mod.rs` |
| Hunted | FLAT `src/*.rs`, `GameState` in `main.rs` | Y (`src/record`) | N | 41 (19) | `audio`, `creatures`, `death`, `debug`, `hud`, `items`, `lighting`, `main`, `player`, `run_timer`, `weapon` |
| moleman-racing | template | N (missing) | N | 13 (2) | `game/mod.rs` |

## Randomness

| Game | RNG sites (file:line) | Shared seeded resource |
|---|---|---|
| cannonball-putt | 0 | — |
| dough-io | `SimRng` (`arena.rs:14-24`) + separate unseeded `FxRng` (`particles.rs:12-18`) | `SimRng` |
| grand-theft-auto-reply | 0 direct | `GameRng` (`game/mod.rs:35-39`) |
| pack-the-ripper | `pack.rs:117`, `confetti.rs:57`, `cards.rs:146` | none |
| sundae-shooter | `juice.rs:35`, `launcher.rs:214,311,322,331`, `level.rs:137,302` | none |
| attic-excavator | `level.rs:69` `rand::rng()` draws the BOARD SEED (non-deterministic); `treasure.rs:42`, `effects.rs:23`; `grid.rs:230` `StdRng::seed_from_u64(seed)` | none |
| gulper | 0 direct | `GameRng` (`game/mod.rs:35-39`) |
| ladder-legend | `notes.rs:220`, `chart.rs:231` `rand::rng()` | none |
| grand-theft-otto | 0 direct | `GameRng` (`game/rng.rs`; doc forbids `rand::rng()`) |
| dive-rise | 0 direct | `GameRng` (`game/mod.rs:44-48`; seeded in `harness/sim.rs:35`) |
| tire-stack | `stack.rs:201,296`, `conveyor.rs:207`, `tires.rs:174` | none |
| BeerPong | 0 | — |
| Gravestone_Gauntlet | `waves.rs:218`, `effects.rs:138`, `enemies.rs:718`, `boss.rs:357` (`rand::rng()`) | none |
| voidrunner | `GameRng`/`SmallRng` (`game/rng.rs`) BYPASSED at `space.rs:48`, `enemies.rs:51` (`starfield.rs:25` is FX-only, fine to leave) | `GameRng`, inconsistently used |
| beat-bender | `driver.rs:129,227` `StdRng::seed_from_u64`; `driver.rs:216` session seed mixed in from `rand::rng()` | partial (chart RNG seeded, session entropy isn't) |
| pizza-pinball | `fx/particles.rs:81`, `pizza.rs:78` | none |
| Hunted | `maze.rs:321`, `audio.rs:227,456`, `creatures.rs:163,486`, `death.rs:203` | none |
| moleman-racing | 0 | — |

## Time, containers, transcendentals

Counts from the survey (`Timer` uses, `delta_secs` calls, `elapsed_secs`
calls, `HashMap`/`HashSet` sites — all lookup-only unless noted — and
`sin`/`cos`/`atan2`/`powf`/`exp`/`ln` call count, a signal for the
native-vs-wasm ulp-drift caveat in `docs/replay-verification.md`).

| Game | Timer | delta_secs | elapsed_secs | HashMap/Set | Transcendental calls |
|---|---|---|---|---|---|
| cannonball-putt | 4 | 10 | 1 | 0 | 13 |
| dough-io | 16 | 22 | 9 | `eyes.rs:5,122` | 28 |
| grand-theft-auto-reply | 5 | 19 | 7 | 0 | 24 |
| pack-the-ripper | 7 | 15 | 9 | 0 | 21 |
| sundae-shooter | 4 | 18 | 7 | 0 | 19 |
| attic-excavator | 9 | 11 | 6 | 0 | 9 |
| gulper | 6 | 31 | 25 | 0 | 59 |
| ladder-legend | 4 | 16 | 2 | 0 | 19 |
| grand-theft-otto | 7 | 32 | 6 | `visuals.rs:7` material cache | 24 |
| dive-rise | 23 | 64 | 26 | `facts.rs:5,228` `SeenCache` | 58 |
| tire-stack | 8 | 14 | 2 | 0 | 11 |
| BeerPong | 11 | 13 | 7 | 0 | 19 |
| Gravestone_Gauntlet | 31 | 17 | 3 | 0 | 5 |
| voidrunner | 23 | 17 | 7 | 0 (test-only) | 35 |
| beat-bender | 4 | 19 | 2 | `music.rs:6,28` (Startup only) | 28 |
| pizza-pinball | 4 | 12 | 6 | 0 | 33 |
| Hunted | 15 | 16 | 5 | 0 | 12 |
| moleman-racing | 4 | 12 | 2 | `voxel/blueprint.rs` `HashSet` dedup ×6 | 22 |

High transcendental counts (Gulper 59, Dive Rise 58, Pizza Pinball 33,
Voidrunner 35) are the games most likely to hit the native-vs-wasm caveat
in step 5 of the skill; budget time for it and know that
`continue-on-error: true` on the CI Node step plus native `--selftest` for
regression coverage is the documented fallback, not a failure to chase
further.

## Wave-2 lessons (2026-09-18)

Three wave-2 ports, three findings general enough to expect again. The
script and checklist changes they produced are in
`tools/rollout-replay.sh` and `port-checklist.md`; what follows is the part
that is about *ports*, not about tooling.

**Attic Excavator — the board seed, and what must NOT move onto `GameRng`.**
`level::board_seed` drew from `rand::rng()`, so the board itself was
non-reproducible and no replay of a real run could ever have verified; it now
draws from `sim::GameRng::next_u64()` with `begin_run` chained ahead of
`setup_level`. The mirror-image judgement is more interesting: `treasure.rs`'s
random toy name became a **pure function of the cell** rather than a
`GameRng` draw. Moving presentation randomness onto the sim stream is the
actual bug — the windowed build would consume a draw the verifier does not,
and every later board desynchronises. The rule: a draw may join `GameRng` only
if **both** builds make it, the same number of times, in the same order.
Particles kept `rand::rng()` behind `allow-forbidden-rng` for the same reason.
Its probe also could not reach the interactions its two `SpawnOrder` sorts
guard (the fixture bot routes around heavy junk and sleeping cats), so the
dependency is written down as two targeted unit tests instead, with the
measurement recorded in the game's `docs/replay-notes.md` so nobody reads the
green probe as evidence the sorts can go.

**Pack The Ripper — animation-named systems that belong in `SimSet`.**
`animate_punch`, `animate_reveal` and `animate_fling` write only
`Transform.scale`/`rotation`, which nothing folds — but each *removes a
component from, or despawns,* an entity the sim queries. The insert side was
already in the chain, so leaving the removal in `Update` would hold packs in
different archetypes in the two apps. It is the archetype, not the value, that
decides: read what a system inserts, removes and despawns, not what it is
called. Second, on fixtures: the selftest's misgrade indices count **grading
presses** and must stay below the number the run actually makes (two at 20 and
45, against 58 at the committed seed). An index past the end is never reached
and nothing fails — the first draft lost a whole Collector Trust of coverage
exactly that way. Re-tuning the ramp or the mash cadence means re-recording
both fixtures.

**Sundae Shooter — the sim was reading `ScreenFade`.** See "The third trap"
under rule 1 in `port-checklist.md`; `tests/windowed_shape.rs` is the
regression, and it is now copied into every game by the rollout script.

## Fleet upgrade lessons (2026-09-18, template d806aa0)

All six ported games were pulled forward to template `d806aa0` and adopted
`tests/windowed_shape.rs` in one pass (the PR numbers are in the status
table). Every probe came back green on `src/` as it stood — no game was
found reading presentation state — so what the pass produced is knowledge
about the *probe*, not about the sims. Four findings, each already folded
back into the template:

**A careful fixture bot under-tests the probe.** This is the big one.
Attic Excavator measured it: over the selftest script's full 1800 ticks
`heavy::wobble_shake` fired **zero** times and `cat::cat_touch` never
connected, because that game's fixture router costs +60 for a cell under
heavy junk and +25 near an awake cat — it avoids by construction exactly the
two windowed-vs-headless divergences worth probing. Two planted bugs (a
*sim* system filtering its query `With<Visibility>`; the checksum folding the
`translation.x` only `wobble_shake` writes) **passed** under the fixture bot
and **failed** only under a second, heavy-seeking one. A probe that never
runs the system it is probing is green for the wrong reason. The template's
probe now ships a two-row bot table with `reckless_script` as a documented
placeholder in the second row, and the checklist's rule 1 has the step:
measure (a counter resource per windowed-only system), and if the fixture bot
is a careful router, write a reckless second script and assert it reaches
them.

**`Option<Res<GameAssets>>` is the biggest resource fork in a real game.**
`ScreenFade` is the trap that shipped, but it is one resource; assets are
where a mid-sized game's two builds actually diverge. Attic Excavator has
nine systems in and around the sim chain taking `Option<Res<GameAssets>>` and
taking the `Some` branch in the browser and the `None` branch in the
verifier, and everything hangs off it: meshes, materials, `Visibility`,
children, backdrops, floating labels, overlays, particle debris. Enumerate
those first when extending `record_windowed` — the fade is the cheap part.

**Carrying `AssetsPlugin` into the probe needs hand-made asset stores.**
`build_headless_app` is `MinimalPlugins` and has no `AssetPlugin`, so a
game's `AssetsPlugin` cannot bake its `GameAssets` there. Insert the stores
it draws from before adding it:

```rust
app.insert_resource(Assets::<Mesh>::default());
app.insert_resource(Assets::<StandardMaterial>::default());
app.add_plugins(AssetsPlugin);
```

That is what lets systems taking a non-`Option` `Res<GameAssets>` (Attic
Excavator's `cat::update_cat_look`) be in the probe at all. Cannonball Putt
is the counter-example and equally worth knowing: its `GameAssets` could not
be carried this way, and what its presence changes there is hole geometry
that `archetype_order`'s faithful mode already materialises — so it was
documented as a deliberate gap rather than faked.

**`toggle_pause` is a tolerated fade read, not a finding.** The template's
`FixedUpdate` pause toggler takes `Option<Res<ScreenFade>>` and refuses a
pause while the fade is busy — the Sundae Shooter shape exactly — and four
ported games carry it verbatim. It is safe only because rule 8 masks
`Buttons::PAUSE` in both `replay::recorder` and `replay::feeder`, so
`pause_just_pressed` is false on every tick of every replay and the fade's
answer is never reached. Written up in the checklist's rule 1 ("The two
tolerated reads") and in the template's own `toggle_pause`; the rollout
script's fade advisory now recognises the shape by its `pause_just_pressed`
edge rather than by the template's name, so it stops firing on every upgrade
of every game.

**CI drift is real and silent.** Two of the six upgrades added the `Cutter
tests` step by hand and the other four did not, leaving games shipping
`tools/test_cut_clips.py` with a green CI that never ran it.
`rollout-replay.sh` now inserts that step (first rollout and `--upgrade`,
guarded on the file being present, idempotent). Expect the same class of
drift elsewhere: `--upgrade`'s workflow diff advisory is the thing that
surfaces it.

## Wave-3 lessons (2026-09-22)

Four ports — Gulper, Ladder Legend, Grand Theft Otto, Dive Rise — and the
first wave in which the *contract* moved rather than only the tooling. Each
finding is written up where a porter will meet it (`port-checklist.md`,
`docs/replay-verification.md`, `tests/windowed_shape.rs`); this is the
index.

**The transcendental rule — never fold what libm produced.** Rule 5 gained
its one exception. Gulper folded `head.facing`
(`velocity.y.atan2(velocity.x)`) and one 5400-tick fixture gave three
verifiers three answers with identical score and ticks: macOS, the Linux CI
runner and wasm. That is not an informational mismatch — `cargo test`'s
`committed_fixture_still_verifies` re-simulates the committed *native*
fixture *natively*, so the job went red on a machine that was not the
porter's. Bisected: ticks 1–7 bit-identical, parting at tick 8. Two halves,
both cheap: **fold no libm result bit-exactly** (it is almost always a pure
function of something folded beside it), and **make a transcendental whose
argument is constant under the fixed tick a literal** — Gulper's
`(-EEL_DRAG * dt).exp()` became `DRAG_PER_TICK`, Dive Rise turned eight
such decays into `*_PER_TICK` constants. Unit-test each literal against the
tick length with a `1e-6` tolerance, never bit-exactly: a bit-exact
assertion pins the porter's own libm, which is the dependency the literal
removes. Measured bit-neutral on Dive Rise. `rollout-replay.sh` now greps
`src/game/` for the five calls and prints a HAND EDIT (advisory) pointing at
the rule; `test_rollout_replay.sh` part (s) covers it.

**What stays is a real limit on where a run may be recorded.** A
varying-argument transcendental (a wander phase, a drift angle) has no
literal to write and can still reach a folded number sideways. Dive Rise's
rendered run parts from Node at tick **44 083** — twelve sim-minutes —
three ticks after a hunter bite folds a `sin`/`cos`-driven hunter position
into the player's knockback; Sundae Shooter's parts at **1056**. So: **only
a wasm-recorded run is guaranteed to verify.** Production is safe (browser
records in wasm, site re-simulates in wasm), and a *native*-recorded run —
a cabinet run above all — may legitimately fail. Verified leaderboards
accepting cabinet submissions is an open question, not a solved one.

**The third trap is wider than "resources".** Dive Rise's
`swim_burst::dash` fell back on `Transform.scale.x.signum()` for the
direction of a dash thrown with no stick held — and that sign is written by
an `AssetsPlugin` `Update` system mirroring the sprite, so it is `+1` for
ever in the verifier. A dash from a standstill while facing left went
opposite ways in the two builds, 0.4667 m, and never re-converged; it fired
several times a run, because taking a draft card *is* A-with-no-stick. All
three probes were green for their own reasons: the fixtures run bare (both
sides read `+1`), `archetype_order` adds components but never writes an
existing one's *value*, and `windowed_shape` carried resources and left the
decorators out. The rule is now **a sim system may not read any state the
windowed build writes and the verifier does not**, resource or component
field; the fix is to latch the thing from `TickInput` into sim-owned state
and let presentation *paint* it. The probe now carries the real
`AssetsPlugin` on hand-made stores (`AssetPlugin` + `init_asset` for
`Shader`/`Mesh`/`ColorMaterial`/`StandardMaterial`) — the stronger version
of the wave-2 `insert_resource(Assets::<T>::default())` note below, and the
one to use, because a plugin that *loads* anything needs an `AssetServer`.

**And a third bot row, because two were structurally not enough.** With
`AssetsPlugin` carried and both shipped bots, Dive Rise's probe still
passed: the forager faced right on all six of its blind dashes and the
diver held `DOWN` every tick so its stick was never zero. The shipped rows
hold a direction on *every* tick, so between them they never produce an
idle-stick tick — and a "do it the way you're facing" fallback fires on
exactly those. The template now ships `coaster_script` (hold LEFT 90 ticks,
release 30, tap A during the release) as the third row. Mutation-checked on
the template itself: a planted `tf.scale.x.signum()` in `move_player`
passes all three rows alone (the template's `AssetsPlugin` is a stub), and
fails the coaster row — and only it — as soon as that plugin is given the
`face_movement` system every real game has.

**A run may not leave `Playing`.** Gulper bounced `Playing → Digest →
Playing` once per band, so `begin_run`/`begin_recording`/`seal_run` fired
per band and the verifier stopped at the first chasm — and the tuning
screen, the biggest single influence on the rest of a run, was an `Update`
system on `GameInput` and was not recorded at all. Dive Rise's draft was
the same shape and worse: the verifier builds no `UiPlugin`, so a draft
opened there would never close. The fix in both: delete the state, make the
phase a **sim resource** driven by `TickInput` inside `SimSet` (Gulper
`run::RunPhase`, Dive Rise's opening shop). A resource and not `SubStates`,
because `NextState` applies once per *frame*. Count the presentation cost
honestly — a `ScreenFade` wipe drives *state* changes, so Gulper's hunt →
tuning transition is a hard cut now; re-add it as a presentation-only
reaction to the phase change, never by routing the phase back through
`GameState`.

**Input outside the canon is unreplayable.** Ladder Legend's
`input::read_input` probed `Query<&Gamepad>` for the four face buttons
because the canon folds South/North into `A` and cannot express a dance-mat
panel — so a run played on the platform's *canonical* controller for that
game recorded lane steps no verifier could reconstruct. Bridge it:
`input::mat_panel_bridge` latches the panels onto `VirtualInput` as
ordinary direction bits in `PreUpdate` `.before(accumulate_input)`, after
which the sim reads no raw input at all. Same game, second half: **a dev
autopilot must end its runs through input.** `drive_autopilot` pinned
`data.health = 0.0` to finish its tour, which makes any recording through
it unverifiable; it now lets missed notes drain HP through the real
game-over path, and that is what made a verified autopilot tour possible.

**Attract mode: register the same functions twice.** Grand Theft Otto's
title screen sits over a living city, and four systems ran in `Menu` as
well as `Playing` behind a custom `scene_live` run condition. All four
write run state the sim reads and two draw from `GameRng`, so none could
stay in `Update` for `Playing`. Resolution: the same four functions
registered **twice** — inside `sim::SimSet` for `Playing`, and in `Update`
gated on `Menu` **alone** for attract mode. Safe only because
`sim::begin_run` reseeds `GameRng` and rewinds `SpawnCounter` on
`OnEnter(Playing)`; pair it with an `OnExit(Menu)` cleanup so no attract
entity survives into the run.

**And the probe's blind spots keep being about what the runs do.** All four
games reported sorts that their `archetype_order` probe could not reach
(Gulper 3, Grand Theft Otto 2, Ladder Legend 1, Dive Rise 1) — two walkers
replanning on one tick, two predators in biting range at once, two morsels
eaten on one tick, an exact timing tie — because the events the sorts order
never occurred under the fixture bot. Attic Excavator's remedy still
applies and was used: a targeted unit test, plus a paragraph in the game's
`docs/replay-notes.md` so nobody reads the green probe as licence to delete
the sort. Dive Rise's `eat_food` is the one that shipped with the argument
written down and no test; it is the wave's known soft spot.

## Wave-4 lessons (2026-09-24)

Five ports — Tire Stack, BeerPong (Table Titans), Gravestone Gauntlet,
Voidrunner, Beat Bender — all merged, all against template `e789fd6`, all
unpublished. The wave produced **one new contract rule**, two rules about
the *bots* rather than the sim, and four honestly-recorded probe gaps. As
with wave 3, each finding is written up where a porter will meet it; this is
the index.

**The new rule: a choice made before `Playing` is not carried either.** Beat
Bender's title screen wrote `song::SelectedSong`/`SelectedDifficulty` and
`driver::setup_match` read them on `OnEnter(Playing)`, so the verifier
re-simulated **every** run on song 0 at NORMAL — a different BPM, segment
count, chart set and AI, from tick one. Every fixture, both probes,
`--selftest` and all three source scans were green; the first *real*
recording was `4 705 recorded ticks → 4 477 re-simulated, matches: false`,
which is a different song's length rather than a rounding error. Same class
as Dive Rise's `MetaSave` draft pool and starting kit, and the reason
nothing local sees it is always the same: **every probe starts from a fresh
`App` and picks the same default the verifier picks**, so they agree by
accident, exactly as they did about the app clock and about the second run.
The rule is now in the contract (`docs/replay-verification.md` rule 7) and
in the checklist as "A choice made before `Playing` is not carried either":
**anything that configures a run is a build constant or an in-run phase
inside `Playing`**, offered from `GameRng`, chosen through `TickInput`, with
its ticks recorded like any other. Beat Bender's picker became a "CHOOSE
YOUR FIGHT" card — a UX change, deliberate, and the only shape in which a
Beat Bender run can be verified at all. Three things carry it forward:
`rollout-replay.sh` now prints a `<file>:<Resource>` advisory for resources
written outside `src/game/` and read inside it (test part (x)); skill step 5
says to record the real run with **every choice moved off its default**; and
Beat Bender's game-owned `tests/selection.rs` is the regression-test pattern
(assert the verifier lands on the *non-default* pick, plus a second test so
that assertion is not vacuous).

**Sub-states keep being the structural cost, and it is always the same
trade.** Three of five ports had one: Voidrunner's `PlayPhase` legs
(corridor → arena → boss), Gravestone Gauntlet's `PlayState`
(Active/Paused/BossIntro) and BeerPong's `ThrowPhase`. `NextState` applies
once per **frame** while the systems that end a phase run once per **tick**,
so at 2-3 ticks per frame the switch landed one to three ticks late in a
frame-rate-dependent way — and in Voidrunner's case the setup mutated run
state (level counter, scroll speed, corridor generator, boss entities) from
outside `SimSet` entirely. All three became a resource plus a
request/apply pair inside the chain. **Pay the presentation cost out loud**:
leg and wave transitions are hard cuts now, the fade sub-state is gone, and
the phase card, music, health bar and starfield follow a resource from
`Update`. BeerPong's version is the one to show a designer — a phase change
now lands inside the tick that decides it, so each turn is deterministically
~2 ticks shorter, and a pause no longer cancels an in-progress charge.

**Two rules about the probe bots, not the sim.** Wave 4 is the first wave
where a probe failure was *in the bot*.

* **Bots must tie-break nearest-target searches on `sim::SpawnOrder`.**
  Voidrunner's `archetype_order` failed on its first run with the sim
  innocent: the corridor's mixed cluster spawns a sentinel and a relay at
  exactly the same `z`, the script picked with `min_by(z)`, and that tie
  resolved on archetype order — so the bot pressed *different buttons* in a
  decorated app. Bisected by decorating one entity kind at a time; fixed by
  tie-breaking on `SpawnOrder` in both `selftest::script` and
  `autopilot::drive_autopilot`. Sorting discipline is not only a rule about
  sim systems.
* **Bots cannot act on latched-only presses in a game that fires on
  release.** BeerPong throws on `primary_just_released`, and `edges()`
  derives `held` from the held set only, so the template's `latched |= A`
  gave **zero throws in 1 800 ticks** on both replacement rows — green
  without ever putting a ball in the air. Holder/reckless/coaster scripts
  must `set_held` the charge and clear it to fire, and the row must assert
  something only a completed action produces. Knock-on: BeerPong's holder
  holds `DOWN`, because holding A would mean never releasing it.

**Four more measured probe gaps, all recorded rather than papered over.**
Tire Stack (0.7-2.2 s between spawns, so no two tires collide on one tick),
BeerPong (capture radius 0.28 against 0.642 between the closest cup centres,
so cup mouths never overlap), Voidrunner (a projectile overlapping two
obstacles does not happen at this hitbox size — and note it is the
*obstacle* sort only: deleting the *projectile* sort in the same system
failed 3 of 12 under `split`, reproducing Dough.io's result on a second
game), and Beat Bender (the sim owns **no entities at all**, so there is no
sort to delete and the probe's stand-ins model a shape the game does not
have). The standing remedy held: when the probe cannot reach a sort, **pin
the measurement with a targeted unit test** — BeerPong's pair is the
clearest version shipped so far, one test asserting the rack geometry and
one handing the system two co-located cups in descending key order — and
say so in `docs/replay-notes.md`. The sentence to write is "the sort stays
because the class is real, not because this probe sees it".

**No native↔wasm drift anywhere in wave 4, on any fixture or any real run** —
including Voidrunner's **35** transcendental call sites and Gravestone
Gauntlet's eight-way boss ring. That is rule 5's discipline working, not
luck, and each game measured it rather than assuming: fold nothing `libm`
produced (Voidrunner folds ship position, drift, hulls, level, leg, scroll
distance and five entity counts, all IEEE-pinned; BeerPong deliberately does
**not** fold the ball's position or velocity, its only libm-derived values,
and folds the ball's *discrete* state instead), and make constant-argument
calls literals with a `1e-6` tolerance test (`TENDRIL_DIRS`, `RING_DIRS`,
`EXPOSED_CORE_DIRS_*`, `STORM_RING`). Tire Stack is the one to watch: its
`stack::stack_top_x` `cos`/`sin` can still reach the checksum, so if
`committed_fixture_still_verifies` ever goes red on CI alone, that is where
to look.

**Every autopilot now ends its tour through input**, in all five games, and
in four of them that is precisely what made the real-run proof possible
(Tire Stack stopped writing `data.lives = 0`, Gravestone dropped its
`health.0 = 2` top-up, Beat Bender stopped writing `ms.ai.hp`/`ms.ended`,
BeerPong disarms its two `MatchData` writes when `GX_REPLAY_DIR` is set).
Two knock-ons for the footage and cover harnesses, which drive the same
tour: Gravestone's tour can now **end early** if the bot takes three hits
before `END_T`, and Beat Bender's is ~15 s longer (BeerPong's is ~95 s
instead of ~65 s when recording). Look at a fresh tour's screenshots before
the next re-record.

**Fixture length is a real cost now, and ticks are the wrong lever.**
Voidrunner's `SELFTEST_TICKS = 7 200` is chosen, not round: the boss leg is
not reached until ~tick 6 240, so a shorter fixture never simulates
`move_boss`, `boss_shoot` or `check_boss_health`. The price is
`archetype_order` at ~107 s (24 cases × 7 200 ticks); Tire Stack's is ~75 s,
BeerPong's pair ~24 s. If a CI job gets tight, **trim seeds, not ticks** —
a shorter fixture stops covering content, a dropped seed only costs a
coincidence.

**Two repo-hygiene items for the owner**, neither caused by these ports:
`games/Gravestone_Gauntlet` has **`dist/` committed** (contrary to the
workspace `CLAUDE.md`), so `bash build_web.sh` dirties ~12 tracked files and
the next person will trip over it; and Gravestone moved `gamebient-input`
v0.2.0 → v0.3.0 as part of the rollout script's pin, worth knowing if
anything else in that repo assumed 0.2.

## Game over, autopilot, input

All games end a run from a `Playing`-gated gameplay system via
`fade.request(GameState::GameOver)` (e.g. Gulper `death.rs:97`, Voidrunner
`collision.rs:269,371`, Cannonball Putt `hole_flow.rs:66`, BeerPong
`scoring.rs:108`) **except Dough.io**, which calls
`next_state.set(GameState::GameOver)` directly (`eating.rs:282`) — see
`references/irregular-games.md`.

Every game has an autopilot; 9 write `GameData` fields directly for
screenshots (dev-only, allowed, but keep that code path out of the
selftest script per rule in `port-checklist.md`): attic-excavator
(`autopilot.rs:213-218`), cannonball-putt (`:378`), dough-io (`:308`),
grand-theft-auto-reply (`:240`), ladder-legend (`:227`), pizza-pinball
(`:268,273`), sundae-shooter (`:197`), tire-stack (`:184`), voidrunner
(`:307-308`). pack-the-ripper's autopilot is read-only.

This section is the 2026-09-16 survey as it stood; ported games have moved
on. Ladder Legend's autopilot no longer pins `health = 0.0` — it ends its
tour through the real game-over path, which is what let the port record a
verifiable autopilot run (see the wave-3 lessons), and every ported game
now ends its run through `sim::end_run` rather than a bare fade request.

All games read `Res<GameInput>` in gameplay (5–11 uses) with 0–2 incidental
raw `ButtonInput<KeyCode>` reads for pause/menu, **except moleman-racing**:
0 `GameInput` uses, 14 raw `ButtonInput<KeyCode>` reads through its own
`Controls` resource (`game/controls.rs:33-44`, populated in `PreUpdate`) —
see `references/irregular-games.md`.
